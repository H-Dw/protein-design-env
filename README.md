# protein-design-env

```bash
# Clone the repository
git clone https://github.com/H-Dw/protein-design-env.git
cd protein-design-env

# Create and activate environment
mamba env create -f environment.yaml
mamba activate proteinae-multimer

# Install the package
pip install -e .
```
